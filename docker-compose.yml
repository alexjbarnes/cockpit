services:
  cockpit:
    build: .
    ports:
      - "3000:3000"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    volumes:
      - ${HOME}:/home/user
      - cockpit-data:/root/.cockpit
    working_dir: /app

volumes:
  cockpit-data:
