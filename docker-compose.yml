services:
  cockpit:
    build: .
    ports:
      - "3000:3000"
    environment:
      - COCKPIT_TOKEN=${COCKPIT_TOKEN}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    volumes:
      - ${HOME}:/home/user
    working_dir: /app
